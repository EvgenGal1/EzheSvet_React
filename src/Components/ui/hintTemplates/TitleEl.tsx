export const TitleEl = ({ text }) => {
  return (
    <span
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
      }}
    >
      {text}
    </span>
  );
};
//export {TitleEl}
