export const TitleEl = ({ text }) => {
  return (
    <span
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        cursor: "pointer",
      }}
    >
      {text}
    </span>
  );
};
//export {TitleEl}
